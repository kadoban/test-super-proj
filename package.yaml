name:                super-proj
version:             0.1.0.0
github:              "kadoban/super-proj"
license:             BSD3
author:              "Joshua Simmons"
maintainer:          "joshua.simmons@emptypath.com"
copyright:           "2017 Joshua Simmons"

# Metadata used when publishing your package
synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on Github at <https://github.com/kadoban/super-proj#readme>

dependencies:
- base >= 4.7 && < 5
- dep-one
- dep-two

executables:
  super-proj:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
